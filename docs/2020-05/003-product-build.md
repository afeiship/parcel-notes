# product build

```shell
# 1. install
npm install parcel-bundler --save-dev

# 2. add task
{
  "scripts": {
    "dev": "parcel <your entry file>",
    "build": "parcel build <your entry file>"
  }
}
```
